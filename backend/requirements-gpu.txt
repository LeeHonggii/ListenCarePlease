# GPU (NVIDIA CUDA) specific dependencies
# Install with: pip install -r requirements-base.txt -r requirements-gpu.txt

# PyTorch for CUDA
torch==2.1.0+cu118
torchaudio==2.1.0+cu118
--extra-index-url https://download.pytorch.org/whl/cu118

# Pyannote (Senko 의존성)
pyannote.audio==3.1.1
pyannote.core==5.0.0

# Senko (Speaker Diarization) - GPU version
# Note: Install from git after other dependencies
# pip install git+https://github.com/narcotic-sh/senko.git[nvidia]

# NeMo (Advanced Speaker Diarization) - Optional
# nemo-toolkit[asr]==2.0.0
# Note: NeMo is OPTIONAL and LARGE (~3GB). Only install if needed:
# pip install nemo-toolkit[asr]

# Additional audio processing
librosa==0.10.1
