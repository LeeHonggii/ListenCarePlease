# STT 내용으로 화자 태깅(~11.14)
## Gliner로 NER(10:00 ~ 11:00)
chunk 옵션을 줘도 사전에 미리 문장 구분하는게 아니라면 4619 중 384개 인식
성공 시에도 A, B, C, D, E, 알겠어요 등을 PERSON으로 인식 -> urchade/gliner_large-v2.1 사용시 5분 소요
후처리 기법 추가(영어, 불용어, xx님, xx님도, score<0.95)
-> 성능 고도화 필요
## 화자 태깅(11:00 ~ 17:50)
1. NER으로 얻은 인물들 GPT 불러와서 태깅
-> CoT 방법을 사용해서 LLM으로 진행(14:00 ~ )
2. NER으로 얻은 인물들 사전학습된 모델을 불러와서 태깅
3. GLINER 반복 활용 화자 태깅(성공 헀으나 정확도 매우 떨어짐)(11:00 ~ 12:30)
-> '화자 식별'보다는 "각 화자가 주로 누구에 대해 이야기하는가?" 또는 **"대화의 중심이 되는 인물(Topic)이 누구인가?"**를 분석하는 데 더 적합
- 3번으로 간다면 성능 고도화 필요, gliner말고 kobert 등 다른 모델 사용 시도
# 중간 결과물 파일들 JSON 형식으로 저장으로 수정(10:00 완료)
-> 11.11일자 개인 노션(정지훈)에 저장 완료